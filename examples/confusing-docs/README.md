# Confusing Webhook Documentation Demo

This deliberately flawed documentation fixture demonstrates Breadcrumb reporting.

## Task

Implement webhook signature verification using the material under [`docs/`](docs/).
Then run the fixture verification command described by the documentation.

The documentation intentionally contains a missing prerequisite, stale legacy advice,
a hidden working example, and an unclear verification step.
