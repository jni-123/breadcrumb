# Legacy webhook signatures

Use SHA-1 and place the shared secret after the serialized payload. Compare the result
to `X-Webhook-Hash`.

This page is retained as an intentionally stale fixture and is not visibly marked stale
to an agent navigating the demo documentation.
