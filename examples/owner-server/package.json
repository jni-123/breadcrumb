{
  "name": "@breadcrumb/owner-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsx src/server.ts",
    "db:migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "@breadcrumb/core": "workspace:*",
    "@breadcrumb/postgres": "workspace:*",
    "@breadcrumb/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
