---
"@breadcrumb/core": patch
"@breadcrumb/server": patch
---

Make zero-install website discovery instructions conditionally actionable at task completion
so agents submit meaningful friction themselves instead of treating reporting as an optional
user action. Allow owners to advertise same-origin protocol and schema documents.
