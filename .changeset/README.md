# Changesets

Add a changeset for user-visible changes to public packages:

```bash
pnpm changeset
```

Use patch releases for fixes, minor releases for backward-compatible features, and major
releases for breaking changes once the protocol leaves its experimental 0.1 phase.
